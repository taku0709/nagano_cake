<div class= "container">
  <div class= "row">
    <div class= "col-12 ">
     <h2>商品詳細</h2>
    </div>
    <div class= "col-5">
       <%= attachment_image_tag @item, :image, :fill, 200, 200, format: 'jpeg', fallback: "no_image.jpg" %>
    </div>
    <div class= "col-7">
      <p>商品名<%= @item.name %></p>
      <p>商品説明<%= @item.introduction %></p>
      <p>ジャンル<%= @item.genre.name %></p>
      <p>税抜（税込）価格<%= @item.price %>(<%= (@item.price * 1.1).floor %>)円</p>
      <p>販売ステータス<% if @item.is_active %>
          販売中
       <% else %>
          販売停止中
       <% end %>
      </p>
      <%= link_to "編集する", edit_admin_item_path(@item), class: "btn btn-success" %>


    </div>
  </div>
</div>