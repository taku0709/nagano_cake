<div class= "container">
 <div class= "row">
  <h2>商品一覧</h2>
   <table class= "table">
    <thead>
     <tr>
       <th>商品ID</th>
       <th>商品名</th>
       <th>税抜価格</th>
       <th>販売ステータス</th>
     </tr>
    </thead>

    <tboby>
     <% @items.each do |item| %>
      <tr>
       <td><%= item.id %></td>
       <td><%= link_to item.name, admin_item_path(item) %></td>
       <td><%= item.price %>円</td>
       <td><%= item.is_active %></td>
      </tr>
     <% end %>
    </tbody>
   </table>
 </div>
</div>